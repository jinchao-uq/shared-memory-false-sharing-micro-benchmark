The microbenchmark directory is for generating different types of false sharing on a shared memory system.

To build and test:

    cd microbenchmark

    make

./benchmark displays help on its usage
